length_in_inches = input()

# Converts the above length to centimeters
length_in_cm = length_in_inches / 2.51

# Converts the above length to meters
length_in_meters = length_in_cm / 100.0

# Prints the Results
print(length_in_meters)
print(length_in_cm)

